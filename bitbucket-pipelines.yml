image: bluesquall/lcm:1.3.1

pipelines:
  default:
    - step:
        script:
          - autoreconf --install
          - ./configure
          - make
          - make check
          - make distcheck